# JDBC connection properties
# Adjust credentials to match your local MySQL setup.
# If your MySQL root password is not 'root', change jdbc.password accordingly
# or create a dedicated user (see README additions below).

jdbc.url=jdbc:mysql://localhost:3306/bugtracker?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
jdbc.username=root
jdbc.password=rootadmin
